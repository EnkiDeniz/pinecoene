import type { Metadata } from "next";
import Link from "next/link";
export const metadata:Metadata = { title:"How it’s made", description:"The nine stages through which a Pinecœne becomes, travels, and can grow without rewriting its past." };
const stages = [["Record","The authorized trail is kept."],["Reading","Events, relations, boundaries, Returns, and candidate Muses are proposed."],["Admission","The owner keeps, rewrites, leaves OPEN, or removes each claim."],["Recognition","Muses are acknowledged separately from evidence."],["Fold","The admitted structure compiles into deterministic geometry."],["Expression & Address","The form gains an authored voice and orientation without changing topology."],["Offering & Locket","An exact permitted projection is placed in a consent vessel."],["Encounter & Return","A recipient may open, inspect, decline, or answer under the granted boundary."],["Successor","A later authorized record may create a new work without rewriting the predecessor."]] as const;
export default function HowPage() { return <main className="plainPublicPage howPage"><header><p className="publicEyebrow">HOW IT’S MADE · THE MACHINERY</p><h1>A record becomes a form.</h1><p>The current public release uses immutable fixtures and browser-local decisions. It does not claim arbitrary source reading or live AI interpretation.</p></header><ol className="makingStages">{stages.map(([title,copy],index) => <li key={title}><span>{String(index+1).padStart(2,"0")}</span><h2>{title}</h2><p>{copy}</p></li>)}</ol><div className="howActions"><Link href="/studio">Enter Studio</Link><Link href="/works/genesis?view=reading">Inspect the Genesis Reading</Link></div></main>; }
